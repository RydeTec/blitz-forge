#!/usr/bin/env bash
set -euo pipefail

HOOK_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "${HOOK_DIR}/.." && pwd)"

case "$(uname -s)" in
  MINGW*|MSYS*|CYGWIN*)
    (
      cd "${REPO_ROOT}"
      cmd.exe /C test.bat
    )
    ;;
  *)
    "${REPO_ROOT}/test.sh"
    ;;
esac
